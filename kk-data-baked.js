// Auto-generated baked data — replace this file (via admin's "Generate Deploy Bundle" button)
// to update the live site's default content for every visitor.
// Initial state: null = no bake yet, site falls back to hardcoded DEFAULTS in admin-store.js.
// IMPORTANT: this fallback only sets KK_DATA_BAKED if it isn't already set by an external
// kk-data-baked.js loaded earlier (so deploy folder's real bake file wins over the bundled copy).
if (typeof window.KK_DATA_BAKED === "undefined") {
  window.KK_DATA_BAKED = null;
}
